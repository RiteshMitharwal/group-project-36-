# Copy to .env.local and restart the frontend (npm run dev).
# Use this if the app cannot reach the backend (e.g. "Cannot reach the server").
# Match the host you use to open the app (localhost or 127.0.0.1).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
